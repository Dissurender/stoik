import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#d7bbc2",
    },
    secondary: {
      main: "#d7bbc2",
    },
    error: {
      main: "#f44336",
    },
  },
});

export default theme;
